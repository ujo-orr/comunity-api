jwt:
  # 테스트용 키. 실제 서버에서는 사용하지 않음
  secret: test-secret-key-must-be-at-least-32-bytes-long
  access-expiration-ms: 3600000
  refresh-expiration-ms: 1209600000

spring:
  flyway:
    enabled: false
  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: create-drop
